name: Call a reusable workflow

on:
  pull_request:

jobs:
  call-workflow-build-clang-doxy:
    uses: ./github/workflows/build-clang-doxy.yml
  call-workflow-release-assets:
    uses: ./github/workflows/release-reusable.yml