# SPDX-FileCopyrightText: Brent Rubell for Adafruit Industries, 2022
#
# SPDX-License-Identifier: MIT
name: WipperSnapper Release CI Caller

on:
  # TODO: Remove below
  pull_request:
  release:

jobs:
  call-workflow-build:
    uses: ./.github/workflows/build-clang-doxy.yml@main
    secrets:
      token: ${{ secrets.TOKEN }}

  call-workflow-release:
    needs: call-workflow-build
    uses: ./.github/workflows/release-reusable.yml@main